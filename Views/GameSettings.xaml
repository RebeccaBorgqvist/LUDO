<Page
    x:Class="LUDO.Views.GameSettings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LUDO.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="#D9D9D9">

    <Grid Background="#666363" CornerRadius="25">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="25"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="25"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Column="1" Grid.Row="1" Grid.ColumnSpan="2" FontSize="36" Foreground="White" TextWrapping="WrapWholeWords" >
				Choose number of players and their colors:
        </TextBlock>

        <StackPanel Grid.Column="1" Grid.Row="2" Orientation="Vertical" Margin="0,20,0,0">
            <Button Content="{Binding Player1Name}" Command="{Binding ChangeNameCommand}" CommandParameter="Player1Name" FontSize="36" Background="#D9D9D9" HorizontalAlignment="Stretch" Margin="0,0,10,10"/>
            <Button Content="{Binding Player2Name}" Command="{Binding ChangeNameCommand}" CommandParameter="Player2Name" FontSize="36" Background="#D9D9D9" HorizontalAlignment="Stretch" Margin="0,0,10,10"/>
            <Button Content="{Binding Player3Name}" Command="{Binding ChangeNameCommand}" Visibility="{Binding IsPlayer3Visible}" CommandParameter="Player3Name" FontSize="36" Background="#D9D9D9" HorizontalAlignment="Stretch" Margin="0,0,10,10"/>
            <Button Content="{Binding Player4Name}" Command="{Binding ChangeNameCommand}" Visibility="{Binding IsPlayer4Visible}" CommandParameter="Player4Name" FontSize="36" Background="#D9D9D9" HorizontalAlignment="Stretch" Margin="0,0,10,10"/>
        </StackPanel>

        <Grid Grid.Column="2" Grid.Row="2">
            <!--Todo. Gabriellas code to replace this grid-->
        </Grid>

        <Grid Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2" Margin="0,5,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Top" Margin="0,0,10,0">
                <Button Background="Transparent" Command="{Binding PlusPlayerCommand}" Visibility="{Binding IsPlusButtonVisible}">
                    <Image Width="52" Height="52" Source="ms-appx:///Assets/plusIcon.png"/>
                </Button>
                <Button Background="Transparent" Visibility="{Binding IsMinusButtonVisible}" Command="{Binding MinusPlayerCommand}">
                    <Image Width="52" Height="52" Source="ms-appx:///Assets/minusIcon.png"/>
                </Button>
            </StackPanel>

            <TextBlock Text="{Binding AddRemoveText}" Grid.Column="1" FontSize="36" Foreground="White" VerticalAlignment="Top" Margin="0,5,0,0"/>
        </Grid>

        <StackPanel Grid.Column="1" Grid.Row="4" Orientation="Horizontal" Grid.ColumnSpan="2" HorizontalAlignment="Center" VerticalAlignment="Bottom">
            <TextBlock FontSize="36" Foreground="White" Margin="0,10,10,0">
					Click → to start the game
            </TextBlock>

            <Button Command="{Binding ForwardCommand}" Background="Transparent">
                <Image Width="68" Height="68" Source="ms-appx:///Assets/forwardIcon.png"/>
            </Button>
        </StackPanel>
    </Grid>
</Page>
