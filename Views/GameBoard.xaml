<Page
    x:Class="LUDO.Views.GameBoard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LUDO.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:xaml="using:Microsoft.Graphics.Canvas.UI.Xaml"
    mc:Ignorable="d"
    xmlns:Interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:Core="using:Microsoft.Xaml.Interactions.Core"
    xmlns:Media="using:Microsoft.Xaml.Interactions.Media" xmlns:xaml="using:Microsoft.Graphics.Canvas.UI.Xaml"

    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    DataContext="{Binding GameBoardViewModelInstance}">

    <Page.Resources>
        <Storyboard x:Name="DiceRollStoryboard">
            <DoubleAnimation
                Storyboard.TargetName="DiceRotateTransform"
                Storyboard.TargetProperty="Angle"
                From="0" To="360" Duration="0:0:2"
                AutoReverse="False" RepeatBehavior="10x"/>
        </Storyboard>
        <Storyboard x:Name="RollDice">
            <DoubleAnimation Duration="00:00:10" Storyboard.TargetName="DiceButton" To="-16.5" Storyboard.TargetProperty="(UIElement.RenderTransform).(CompositeTransform.TranslateX)"/>
            <DoubleAnimation Duration="00:00:10" Storyboard.TargetName="DiceButton" To="1.5" Storyboard.TargetProperty="(UIElement.RenderTransform).(CompositeTransform.TranslateY)"/>
        </Storyboard>
    </Page.Resources>


        <xaml:CanvasControl x:Name="GameBoardCanvas" Width="1000" Height="800" />
        <TextBlock HorizontalAlignment="Left" Name="counter" Margin="42,649,0,0" TextWrapping="Wrap" Text="TextBlock" VerticalAlignment="Top"/>

    <Grid HorizontalAlignment="Right" Width="1000" Height="800" VerticalAlignment="Center" Margin="0,0,250,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="983*"/>
            <ColumnDefinition Width="17*"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>

        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>

            <RowDefinition Height="200*"/>
            <RowDefinition Height="143*"/>
            <RowDefinition Height="19*"/>
            <RowDefinition Height="38*"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>

        </Grid.RowDefinitions>

        <xaml:CanvasControl x:Name="GameBoardCanvas" Grid.RowSpan="4" RenderTransformOrigin="0.499,0.602" >
            <xaml:CanvasControl.RenderTransform>
                <CompositeTransform SkewX="0" TranslateX="0" SkewY="0" TranslateY="0"/>
            </xaml:CanvasControl.RenderTransform>
        </xaml:CanvasControl>

        <!-- dice section -->
        <Button x:Name="DiceButton" 

                Grid.Column="{Binding CurrentPositionColumn}"
                Command="{Binding RollDiceCommand}"
                Background="Transparent"
                Width="43"
                Height="43"
                Padding="0"
                BorderThickness="0" Margin="377,387,0,0" RenderTransformOrigin="0.465,0.504" VerticalAlignment="Top" Grid.RowSpan="2">

            <Button.RenderTransform>
                <CompositeTransform/>
            </Button.RenderTransform>

            <Image Source="{Binding CurrentDiceImage}"
                   Stretch="UniformToFill">
            </Image>
        </Button>
    </Grid>
</Page>
